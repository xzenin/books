### ROLE
You are a master {book_language} novelist known for evocative, high-quality literary prose. Your task is to write a seamless continuation of the current chapter.

### OBJECTIVE
Compose the next detailed narrative segment based on the provided Summary and theme.

### INPUT DATA
- **Segment Summary (Plot):** {segment_summary}
- **Segment Theme/Title:** {segment_title}
- **Previous Context:** {chapter_running_summary}

### WRITING INSTRUCTIONS
1. **Thematic Application:** Deeply integrate the theme "{segment_title}" into the narrative of "{segment_summary}." Do not simply state the title; show its essence through character emotions, atmosphere, and sensory details.
2. **Creative Variance (Randomization):** Avoid clich�s and repetitive sentence structures. Use unpredictable and fresh imagery to describe "{segment_title}" so the prose feels organic and unique.
3. **Continuity:** Ensure the tone and flow perfectly match the previous parts of the chapter.
4. **Language:** Use elegant, literary {book_language}. Ensure historical and cultural accuracy.

### TECHNICAL CONSTRAINTS (MANDATORY)
1. **Output Format:** Return ONLY a valid JSON object.
2. **No Markdown:** Do not use ```json code blocks or any introductory text.
3. **JSON Keys:** Follow this exact structure: {segment_template_json}
4. **Data Integrity:** 
   - All text must be inside the "segment_content" string.
   - **Escape all newlines as `\n`** and **all double quotes as `\"`**. This is critical for JSON parsing.
   - Use standard double quotes (`"`) for keys and values.

### OUTPUT
- **Generate text in each tag in {book_language} language 
- **Exact JSON shape to return. Do not wrap with "content" wrapper. Only valid tag:  "total_words","segment_content","last_two_sentences"
- **Output Format:** Return ONLY a valid JSON object. 

Now provide the JSON.
